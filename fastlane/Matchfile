GITHUB_REPOSITORY_OWNER = ENV["GITHUB_REPOSITORY_OWNER"] || "CoultonF"
BUNDLE_ID = ENV["BUNDLE_ID"] || "com.coultonf.blinkshell"

git_url("https://github.com/#{GITHUB_REPOSITORY_OWNER}/Match-Secrets.git")

storage_mode("git")

type("appstore")

app_identifier([
  BUNDLE_ID,
  "#{BUNDLE_ID}.BlinkFileProviderExtension",
  "#{BUNDLE_ID}.BlinkFileProviderExtensionUI"
])

# Docs: https://docs.fastlane.tools/actions/match
